package com.rfidsoftwares.integration.config

/**
 * Shopify template endpoint paths.
 *
 * Phase 3 does not implement Shopify adapter behavior; this object exists for
 * consistent path conventions when Shopify integration is added in later phases.
 */
object ApiPathShopifyTemplate {
    // Example placeholders (to be replaced with real backend contract paths later).
    const val BASE: String = "/shopify"
}

